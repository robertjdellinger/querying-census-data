# Auto detect text files and perform LF normalization
* text=auto
*.shp* filter=lfs diff=lfs merge=lfs -text
Data/Processed/*.shp !text !filter !merge !diff
Data/Processed/*.dbf !text !filter !merge !diff
